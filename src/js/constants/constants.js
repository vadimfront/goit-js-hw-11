const searchForm = document.querySelector('.search-form');
const galleryWrapper = document.querySelector('.gallery');
const btnLoadMore = document.querySelector('.load-more');

export { searchForm, galleryWrapper, btnLoadMore };
